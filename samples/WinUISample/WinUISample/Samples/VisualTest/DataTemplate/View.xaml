<UserControl
    x:Class="WinUISample.VisualTest.DataTemplate.View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:vms="using:ViewModelsSamples.VisualTest.DataTemplate"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <vms:ViewModel/>
    </UserControl.DataContext>

    <ItemsControl ItemsSource="{Binding Departments}">
        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vms:DepartmentInfo">
                <Grid RowDefinitions="Auto,300">

                    <TextBlock
                        Grid.Row="0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Text="{Binding DepartmentName}"/>

                    <lvc:CartesianChart
                        Grid.Row="1"
                        SeriesSource="{Binding Data}">

                        <lvc:CartesianChart.SeriesTemplate>
                            <DataTemplate x:DataType="vms:ChartData">
                                <lvc:XamlLineSeries
                                SeriesName="{Binding SeriesName}"
                                Values="{Binding Values}"/>
                            </DataTemplate>
                        </lvc:CartesianChart.SeriesTemplate>

                    </lvc:CartesianChart>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</UserControl>

<UserControl x:Class="UnoPlatformSample.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Navigation region for Factos UI-test builds. Factos mounts its FactosShell here via the
       root route; FactosShell in turn hosts each test's view. Used only when UITesting=true. -->
  <ContentControl x:Name="Splash"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch"
                  VerticalContentAlignment="Stretch" />

</UserControl>
